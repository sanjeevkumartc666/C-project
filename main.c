#include <stdio.h>

// Function Prototypes:
// These lines declare that the functions exist elsewhere.
void biggest3(); 
void factorial();
void palindrome();
// Explicit return type 'int' for main (Standard C)
int main () {
    // You probably want to pass arguments to these functions or handle input inside them
    biggest3();
    factorial();
    palindrome();
    return 0; // Return 0 to indicate successful execution
}

